sudo: required
dist: trusty
addons:
  chrome: stable

language: node_js
node_js:
  - node

branches:
  only:
    - master

before_install:
  - npm i
  - cd demo
  - npm run build

script:
  - npm test

notifications:
  email:
    on_failure: always
    on_success: change
